import { AxiosInstance } from 'axios';
import { ICard } from '../interfaces';
/**
 * Get list of all available cards.
 *
 * @param {AxiosInstance} apiClient
 */
declare const getCards: (apiClient: AxiosInstance) => Promise<ICard[]>;
export { getCards };
